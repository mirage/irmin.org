(rule
 (targets mdx.ml)
 (deps
  (:markdown
   (glob_files *.md)))
 (action
  (with-stdout-to
   %{targets}
   (bash "echo %{markdown} | xargs --max-args=1 ocaml-mdx pp"))))

(test
 (name mdx)
 (libraries irmin irmin-unix lwt hiredis))
